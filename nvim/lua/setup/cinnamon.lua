require('cinnamon').setup({
  extra_keymaps = true,
  extended_keymaps = true,

  always_scroll = false,
  centered = true,
  default_delay = 5,
  hide_cursor = true,
  max_length = -1,
  scroll_limit = -1,
})
